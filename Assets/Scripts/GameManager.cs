using UnityEngine;

public class GameManager : MonoBehaviour
{
    private Cube ally;
    private Cube enemy;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void FixedUpdate()
    {
        // each turn, each block gets to attack
    }
}
